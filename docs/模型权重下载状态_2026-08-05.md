# 模型权重下载状态（2026-08-05 暂停点）

## 当前状态

下载已按用户要求暂停，所有 `python` 和 `curl` 下载进程均已停止。未删除任何完整权重、断点文件或下载缓存。

| 模型 | 状态 | 有效权重/断点 | 说明 |
|---|---|---:|---|
| `bigscience/bloom-3b` | 已完成 | 5.593 GiB | `model.safetensors` 完整，模型 manifest 已生成 |
| `facebook/xglm-2.9B` | 可续传 | 4.849 GiB / 5.479 GiB | 已完成 88.50%，剩余约 0.630 GiB |
| `mistralai/Mistral-7B-v0.1` | 未开始 | 0 | 等待重新启动 |
| `CohereLabs/aya-23-8B` | 未开始 | 0 | 等待重新启动；仍需账户具备 gated repo 权限 |

### BLOOM-3B

- 目录：`langhub_models/bigscience__bloom-3b`
- revision：`52bc5b43010b4844513826b8be3f78c7344c37d7`
- 有效权重：`model.safetensors`
- manifest：`.lht_model_manifest.json`
- 状态：完整，不需要重新下载。

目录当前总占用约 15.135 GiB，其中约 9.5 GiB 是早期失败下载留下的隐藏缓存和 `.incomplete` 文件。它们不是有效权重，也不应上传。为遵守当前“先保存现场”的要求，本次暂停时没有删除；确认继续后，可在核验完整权重后安全清理。

### XGLM-2.9B

- 目录：`langhub_models/facebook__xglm-2.9B`
- revision：`33c659ae27de09c0a54123d3902dac48cbb8592a`
- 断点文件：`pytorch_model.bin.part`
- 已下载：`5,206,534,242` bytes
- 预期大小：`5,883,279,159` bytes
- 完成比例：`88.50%`
- 剩余：`676,744,917` bytes，约 0.630 GiB

不要移动、改名或删除 `pytorch_model.bin.part`。续传脚本会使用 HTTP Range 从当前字节继续，完成后自动改名为 `pytorch_model.bin` 并写入 manifest。

## 重新开始命令

在项目根目录的 PowerShell 中执行：

```powershell
$env:HF_HUB_DISABLE_XET="1"
$env:PYTHONUNBUFFERED="1"

& ".\.venv-model-download\Scripts\python.exe" `
  ".\src\download_model_weights_resumable.py" `
  --suite ".\configs\model_suite_literature_additions_random200.json" `
  --output-root ".\langhub_models" `
  --max-retries 100
```

脚本会：

1. 跳过已经完整的 BLOOM-3B；
2. 从 XGLM 的 88.50% 断点继续；
3. 完成后依次下载 Mistral-7B-v0.1 和 Aya-23-8B；
4. 为每个模型写入 `.lht_model_manifest.json`；
5. 最终生成 `portable_models_manifest.json`。

## 当前磁盘

- D 盘暂停时剩余约 244.98 GiB。
- 空间足够完成其余模型。
- 在四个模型全部核验前，不清理 `.part` 文件。
- 完成后可以清理隐藏 `.cache/huggingface/download/*.incomplete`，减少上传体积。

## 已新增项目文件

- `configs/bloom_3b_week1.json`
- `configs/xglm_2_9b_week1.json`
- `configs/mistral_7b_v01_week1.json`
- `configs/aya_23_8b_week1.json`
- `configs/model_suite_literature_additions_random200.json`
- `src/download_model_weights_resumable.py`
- `docs/文献对齐模型选择与AutoDL迁移指南.md`

